## STRING MANIPULATION
- How to define a string in three ways - single quote / double quote / backticks?
- How can you split a string into two or more parts when assigning it to a variable?
- What is the length of an empty string (w/ and w/o space)?
- What is the purpose of string or template literals?
    - String interpolation
    - Embedded HTML code
    - Multi-line string declaration
    - Easily append single / double quotes in a string w/o using escape sequences
- How can you embed HTML code within JavaScript?
- How can strings or characters be compared in javascript?
- What is string interpolation?
- What is string pool and string interning? 
    - For a particular value of a string, there will be only one copy of string that exist in the memory.
    - Benefits - To make string comparison faster and Memory Optimization.
- Whether the string be will created in the stack or heap segment?
- Is there any difference between toString() vs String()?
    - toString() cannot be used on null or undefined, and will throw an error.

- Output: Give the correct answer - What happens in this case?
    - let s1 = "Apple"
    - let s2 = "Apple"
    - let s3 = new String("Apple")